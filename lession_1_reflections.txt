did viewing a diff between two virsions of a file help you see the bug that introduced?

	fill in your answer here

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?

	I'll use version control system to access old versions of file/prigram.

What do you think are the pros and cons of manually choosing when the create a commit, like you do in Git, vs having versions automatically saved, like Google docs does?

	this reduses the unnecessry short commits so to reduce chunck of history

Why do you think some version control systems, like Git, allow saving multiple files in one commit, while others, like Google Docs, treat each file separately?

    this is what git does

How can you use the commands git log and git diff to view the history of files?
    go to the folder and type below command 
    git log 
    git diff first_file_ID second_file_ID

How might using version control make you more confident to make changes that
could break something?

    version control has the history and can be checkedout anytime to see the previous versions of codes to know where was the bug intrudeced. 

Now that you have your workspace set up, what do you want to try using Git for?

    Fill in your answer here
